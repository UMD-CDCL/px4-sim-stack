# Integration Result

Accepted: px4sim front-door restart/UI slice and verification stage wrappers.

Rejected: generated MAVInsight-only parser change because it is ignored and cannot travel with a clean checkout.

Conflicts: none. Existing user ledger edits were preserved.

Verification pending at the integration commit: `./px4sim check`, Python compilation, shell syntax, dry-run wrappers, and workflow artifact validation.
