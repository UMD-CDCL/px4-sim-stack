# Packet: runtime features

Owner: Herschel
Status: rejected for integration

The proposed strict `fiducial_lla` parser change was made only under the ignored `.build-contexts/mavinsight` generated tree. It is not a portable tracked-repository change and is therefore excluded from this commit. The packet reported parser assertions and Python compilation, but `pytest` was unavailable.

Follow-up: locate the tracked MAVInsight source or update the build-context ownership before implementing this feature.
